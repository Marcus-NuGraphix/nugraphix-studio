# Skill 02 — Implementation Protocol

## Step order (non-negotiable)

1. Schema + migrations (if needed)
2. Server functions (Zod + auth + ServerResult)
3. UI shell (route + layout + placeholders)
4. UI wiring (TanStack Query)
5. Empty/loading/error states
6. Logging + security review
7. Minimal tests (where applicable)

## Rules

- One step per task request.
- No broad refactors.
- No new libraries unless explicitly requested.
- Provide minimal diffs touching only scoped files.
